{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "cmake build",
      "command": "bash",
      "args": [
        "-c",
        "mkdir -p build install && cmake -S ${workspaceFolder}/bundle_adjustment/simple_example -B ${workspaceFolder}/build -DCMAKE_BUILD_TYPE=Debug -DCMAKE_INSTALL_PREFIX=${workspaceFolder}/install && cmake --build ${workspaceFolder}/build -j8 && cmake --install ${workspaceFolder}/build"
      ],
      "type": "shell",
    },
    {
    "label": "cmake build bal",
    "type": "shell",
    "command": [
      "cd ${workspaceFolder}/ba_uni_washington/build/ && cmake .. -DCMAKE_BUILD_TYPE=Debug -DCMAKE_INSTALL_PREFIX=${workspaceFolder}/ba_uni_washington/install && make -j8 && make install"
    ]
  },
  ]
}